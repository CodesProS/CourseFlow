# Vercel-hosted frontend points at the Render-hosted API.
# In Vercel, set this in Project Settings → Environment Variables (don't commit real values).
VITE_API_BASE_URL=https://courseflow-api.onrender.com
